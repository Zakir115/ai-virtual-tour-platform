{
  "name": "ai-virtual-tour-root",
  "version": "1.0.0",
  "scripts": {
    "install-client": "cd client && npm install --legacy-peer-deps",
    "install-server": "cd server && npm install",
    "build": "npm run install-client && npm run install-server && cd client && npm run build"
  },
  "engines": {
    "node": "18.x"
  }
}